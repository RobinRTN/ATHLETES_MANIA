<!DOCTYPE html>
<html>
  <head>
    <title>ATHLETESMania</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="_index.css" media=”screen” />
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
    <the-script src="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.js"></the-script>
  </head>

  <body>
  <div id="content">
    <% if current_page?(controller: 'pages', action: 'home') %>
      <%= render "shared/navbarhome" %>
    <% elsif current_page?(controller: 'athletes', action: 'index') %>
      <%= render "shared/navbarindex" %>
    <% else %>
      <%= render "shared/navbar" %>
    <% end %>
    <%= yield %>
  </div>
  <%= render "shared/footer"%>
  </body>
</html>
